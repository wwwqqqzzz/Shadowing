page { background: var(--color-bg-base); }
.container { padding: 32rpx; min-height: 100vh; }

/* ─── Progress ─────────────────────────────────────────── */

.progress {
  color: var(--color-text-secondary);
  font-size: 26rpx;
  margin-bottom: 32rpx;
}

/* ─── Sentence card ────────────────────────────────────── */

.sentence-card {
  background: var(--color-bg-elevated);
  border-radius: 16rpx;
  padding: 32rpx;
  margin-bottom: 32rpx;
}

.sentence-text {
  display: block;
  color: var(--color-text-primary);
  font-size: 32rpx;
  line-height: 1.6;
  font-weight: 500;
  margin-bottom: 16rpx;
}

.sentence-translation {
  display: block;
  color: var(--color-text-secondary);
  font-size: 26rpx;
  line-height: 1.5;
  margin-bottom: 12rpx;
}

.sentence-material {
  display: block;
  color: var(--color-text-tertiary);
  font-size: 22rpx;
  margin-top: 8rpx;
}

/* ─── Controls ─────────────────────────────────────────── */

.controls {
  display: flex;
  justify-content: center;
  margin-bottom: 32rpx;
}

.play-btn {
  width: 96rpx;
  height: 96rpx;
  border-radius: 50%;
  background: var(--color-brand);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36rpx;
  color: var(--color-bg-base);
}

.play-btn.playing {
  background: var(--color-warning);
}

/* ─── Recording ─────────────────────────────────────────── */

.recording-area {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 16rpx;
  padding: 32rpx 0;
}

.rec-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background: var(--color-error);
  animation: recPulse 1s ease-in-out infinite;
}

@keyframes recPulse {
  0%, 100% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.4; transform: scale(0.8); }
}

.rec-time {
  color: var(--color-error);
  font-size: 28rpx;
  font-weight: 600;
}

/* ─── Feedback ──────────────────────────────────────────── */

.feedback {
  background: var(--color-bg-elevated);
  border-radius: 16rpx;
  padding: 32rpx;
}

.score {
  font-size: 72rpx;
  font-weight: 700;
  text-align: center;
  margin-bottom: 24rpx;
}

.score-low { color: var(--color-error); }
.score-mid { color: var(--color-warning); }
.score-ok { color: var(--color-brand); }

.one-move {
  display: block;
  text-align: center;
  color: var(--color-brand);
  font-size: 24rpx;
  margin-bottom: 20rpx;
}

.missing-words {
  margin-bottom: 24rpx;
}

.missing-label {
  display: block;
  color: var(--color-warning);
  font-size: 24rpx;
  margin-bottom: 12rpx;
}

.word-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
}

.word-tag {
  background: var(--color-bg-elevated);
  color: var(--color-error);
  font-size: 24rpx;
  padding: 6rpx 16rpx;
  border-radius: 8rpx;
}

.text-compare {
  margin-bottom: 32rpx;
}

.compare-row {
  margin-bottom: 16rpx;
}

.compare-label {
  display: block;
  color: var(--color-text-secondary);
  font-size: 22rpx;
  margin-bottom: 4rpx;
}

.compare-text {
  display: block;
  color: var(--color-text-primary);
  font-size: 26rpx;
  line-height: 1.5;
}

.feedback-actions {
  display: flex;
  gap: 24rpx;
}

.retry-btn {
  flex: 1;
  text-align: center;
  padding: 20rpx 0;
  border-radius: 24rpx;
  background: var(--color-bg-elevated);
  color: var(--color-text-secondary);
  font-size: 28rpx;
  font-weight: 500;
}

.next-btn {
  flex: 1;
  text-align: center;
  padding: 20rpx 0;
  border-radius: 24rpx;
  background: var(--color-brand);
  color: var(--color-bg-base);
  font-size: 28rpx;
  font-weight: 600;
}

.btn-tap { opacity: 0.8; }

/* ─── Finished ──────────────────────────────────────────── */

.finished {
  padding-top: 100rpx;
  text-align: center;
}

.finished-emoji {
  display: block;
  font-size: 96rpx;
  margin-bottom: 16rpx;
}

.finished-title {
  display: block;
  color: var(--color-brand);
  font-size: 40rpx;
  font-weight: 700;
  margin-bottom: 48rpx;
}

.summary-cards {
  display: flex;
  gap: 20rpx;
  margin-bottom: 40rpx;
}

.summary-card {
  flex: 1;
  background: var(--color-bg-elevated);
  border-radius: 16rpx;
  padding: 28rpx 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6rpx;
}

.summary-num {
  font-size: 52rpx;
  font-weight: 700;
  color: var(--color-text-primary);
}

.summary-num.ok { color: var(--color-brand); }
.summary-num.mid { color: var(--color-warning); }
.summary-num.low { color: var(--color-error); }

.summary-label {
  font-size: 22rpx;
  color: var(--color-text-secondary);
}

.finished-change {
  background: var(--color-bg-elevated);
  border-radius: 16rpx;
  padding: 20rpx 32rpx;
  margin-bottom: 32rpx;
}

.change-detail {
  color: var(--color-brand);
  font-size: 26rpx;
  font-weight: 500;
}

.finished-actions {
  display: flex;
  gap: 24rpx;
  margin-top: 48rpx;
}

.finished-actions .retry-btn {
  flex: 1;
  text-align: center;
  padding: 20rpx 0;
  border-radius: 24rpx;
  background: var(--color-bg-elevated);
  color: var(--color-text-secondary);
  font-size: 28rpx;
}

.finished-actions .back-btn {
  flex: 1;
  text-align: center;
  padding: 20rpx 0;
  border-radius: 24rpx;
  background: var(--color-brand);
  color: var(--color-bg-base);
  font-size: 28rpx;
  font-weight: 600;
}